# Prisma + PostgreSQL Starter Pack

#### Make sure you have installed Prisma
`npm install -g prisma`

#### Make sure your docker service is running

`docker compose up -d`

#### Write your model in file datamodel.prisma

#### Deploy the model
`prisma deploy`